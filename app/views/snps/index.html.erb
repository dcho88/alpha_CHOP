<h1>Listing snps</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @snps.each do |snp| %>
  <tr>
    <td><%= snp.name %></td>
    <td><%= snp.content %></td>
    <td><%= link_to 'Show', snp %></td>
    <td><%= link_to 'Edit', edit_snp_path(snp) %></td>
    <td><%= link_to 'Destroy', snp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Snp', new_snp_path %>
