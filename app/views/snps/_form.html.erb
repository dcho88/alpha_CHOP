<%= simple_form_for(@snp, html: {class: "form-horizontal"}) do |f| %>
  <% if @snp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@snp.errors.count, "error") %> prohibited this snp from being saved:</h2>

      <ul>
      <% @snp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= f.input :content %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
